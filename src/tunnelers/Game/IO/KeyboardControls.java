package tunnelers.Game.IO;

import tunnelers.core.io.AControls;

/**
 *
 * @author Stepan
 */
public class KeyboardControls extends AControls {

	public KeyboardControls(byte schemeId) {
		super(schemeId);
	}
}
